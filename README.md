Chrome Plugin that runs once per second, checking to see if any links match a given pattern.

If the pattern is matched, overwrite the parent element with a note saying that the link has been removed.
